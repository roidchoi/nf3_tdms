# 인터페이스 정의 (interfaces.md)

> **Sub Project**: P{n}_{Name} **마지막 업데이트**: {YYYY-MM-DD} (Task-{ID}) **기록 원칙**: 요약 금지. 정확한 타입, 컬럼명, 경로, 라인 번호 전체 기재.

---

## 사용 지침

- 이 파일의 정보로 다음 Task 구현 시 스키마를 **추정하지 않아도** 된다
- 변경 발생 즉시 업데이트 (Task 완료까지 미루지 않음)
- 삭제된 함수/테이블은 `~~취소선~~`으로 표시 후 Lint 시 제거

---

## DB 스키마

### {테이블명}

> 마지막 변경: Task-{ID} | 위치: `{DB 파일 경로}`

|컬럼명|SQL 타입|Python 타입|설명|제약|
|---|---|---|---|---|
|{col}|TEXT / INTEGER / REAL / BLOB|str / int / float|{설명}|PK / NOT NULL / —|

**인덱스**:

- `{인덱스명}`: ({컬럼}) — {용도}

---

## 함수/메서드

<!-- 함수 항목 템플릿 --> <!-- ### {함수명}() > 위치: `{Sub_Project}/path/to/file.py:{라인번호}` > 마지막 변경: Task-{ID} **입력**: | 파라미터 | 타입 | 설명 | 기본값 | |----------|------|------|--------| | {param} | {type} | {설명} | {값 또는 필수} | **출력**: - 타입: `{반환 타입}` - 형식: `{컬럼 목록 또는 구조 설명}` **주의사항**: - {에러 참조: errors.md#{ERR-ID} 등} **사용 예시**: ```python {예시 코드} ``` -->

---

## 데이터 파이프라인 입출력

### {파이프라인명}

```
입력: {형식 및 경로}
  ↓ {변환 내용}
출력: {형식 및 경로}
```

|단계|입력 타입|출력 타입|핵심 컬럼|
|---|---|---|---|
|{단계명}|{타입}|{타입}|{컬럼목록}|

---

## 설정 파일

|파일|경로|주요 설정 키|
|---|---|---|
|{파일명}|`{경로}`|{키 목록}|

---

## Sub Project 간 인터페이스

> 이 Sub Project가 다른 Sub Project와 주고받는 데이터

### 출력 (→ {대상 Sub Project})

- 형식: {타입}
- 경로/방식: {설명}
- 관련: `parent_wiki/architecture.md#{섹션}`

### 입력 (← {출처 Sub Project})

- 형식: {타입}
- 경로/방식: {설명}