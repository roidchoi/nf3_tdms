# 코드베이스 맵 (codebase_map.md)

> **Sub Project**: P{n}_{Name} **마지막 업데이트**: {YYYY-MM-DD} (Task-{ID}) **기록 원칙**: "현재 상태"만 기재. 미래 계획 혼재 금지. 상태 표시 필수.

---

## 1. 현재 폴더 구조

```
P{n}_{Name}/
├── {레이어 or 폴더명}/    # {역할} {✅완성 / 🔄진행중 / ⬜미착수}
│   ├── {파일명}.py        # {한줄 역할}
│   └── ...
├── tests/
│   ├── unit/
│   └── integration/
├── config/               # 설정 파일
├── ops/                  # 운영 스크립트
└── {기타}/
```

---

## 2. 핵심 데이터 흐름

```
{입력 소스}
    │ {수집/로드 방법}
    ▼
{중간 처리} → {저장소}
    │
    ▼
{출력 형식}
```

---

## 3. 모듈별 상태 및 역할

|모듈/폴더|상태|핵심 파일|역할 요약|
|---|---|---|---|
|{모듈명}|✅/🔄/⬜|{파일명}.py|{역할}|

---

## 4. 핵심 진입점 (Entry Points)

|스크립트|실행 방법|역할|
|---|---|---|
|`ops/{파일}.py`|`python ops/{파일}.py`|{역할}|

---

## 5. 테스트 현황

|테스트 파일|커버 대상|상태|
|---|---|---|
|`tests/unit/{파일}.py`|{대상}|✅통과 / ⚠️실패 / ⬜미작성|

---

## 6. 가비지 현황

> Lint 시 사용자 승인 후 삭제. 이 섹션이 비어있는 것이 정상.

|파일/폴더|생성 Task|삭제 사유|
|---|---|---|
|—|—|—|

---

## 7. 변경 이력 요약

|Task|주요 변경 내용|
|---|---|
|Task-{ID}|초기 구조 생성|