# TDMS .env 샘플 (환경변수 참고용)

> **⚠️ 주의**: 이 파일은 샘플입니다. 실제 `.env` 파일에 값을 채워 사용하세요.
> `.env`는 `.gitignore`에 반드시 포함되어야 합니다. **절대 Git에 커밋하지 마세요.**

---

## .env 위치 결정

**프로젝트 루트(`01_nf3_tdms/.env`)를 권장합니다.**

| 위치 | 장점 | 단점 |
|---|---|---|
| **프로젝트 루트** ✅ | 모든 서브프로젝트가 단일 `.env` 공유. 환경변수 중복 없음. `GITHUB_PAT` 등 전역 값과 통합 관리 가능 | 서브프로젝트 간 변수 네임스페이스 충돌 주의 필요 |
| `tdms_core/` | 구현 코드와 가깝게 위치 | `p1_shared`·`p2_kdms`·`p3_usdms`·`p4_manager`가 각기 다른 경로에서 `.env` 로드 경로를 직접 지정해야 함. 중복·불일치 위험 |

**결론**: `python-dotenv`의 `find_dotenv()`를 사용하면 상위 디렉토리를 자동으로 탐색하므로, 루트에 단일 `.env`를 두는 것이 가장 관리하기 쉽습니다. 서브프로젝트 코드에서는 `load_dotenv(find_dotenv())` 한 줄로 통일 사용.

---

## Github

```env
# Github Personal Access Token
GITHUB_PAT=your_token_here
```

---

## [p1_shared] 환경 감지 설정 (T-002: EnvDetector)

> **💡 내부망 IP(192.168.x.x) 사용 필수 안내 및 주의사항**
> - **목적**: 개발PC와 서버PC가 서로의 DB에 접근(동기화)하려면 `localhost`나 WSL 가상 IP(`172.x.x.x`)로는 통신이 불가능하므로, 동일 공유기에 연결된 Windows 호스트의 실제 내부망 IP를 사용해야 대칭적 접속이 가능합니다.
> - **조회 방법**: WSL에서 `hostname -I`를 치면 가상 IP만 나옵니다. 터미널에서 아래 명령을 실행하여 실제 Windows IP를 확인하세요.
>   `powershell.exe -Command "ipconfig" | grep -A 5 "Wi-Fi" | grep "IPv4"`
> - **코드 구현 및 설정 고려사항**:
>   1. **EnvDetector 구현 (T-002)**: 무선 LAN 특성상 재부팅 시 IP가 변경될 수 있습니다. 따라서 환경 감지 로직은 `IP` 매칭보다 불변하는 `HOSTNAME` 매칭을 최우선 순위로 두어 구현해야 합니다.
>   2. **접속 허용 설정**: 상대 DB 접속을 위해 양쪽 PC의 **PostgreSQL `pg_hba.conf`**(`host all all 192.168.0.0/16 md5` 등)와 **Windows 방화벽 인바운드 규칙(5432, 5433 포트)**에서 내부망 접속을 반드시 허용해야 합니다.

```env
# ─────────────────────────────────────────────────────
# 환경 자동 감지 설정
# TDMS_ENV를 명시하면 hostname/IP 자동 감지를 무시합니다.
# 값: dev | server | (비워두면 자동 감지)
# ─────────────────────────────────────────────────────
TDMS_ENV=

# 개발PC 식별 정보 (WSL2 내부가 아닌 Windows 호스트의 정보 입력)
DEV_HOSTNAME=ROID-DEV
DEV_IP=192.168.1.10

# 서버PC 식별 정보
SERVER_HOSTNAME=ROID-SERVER
SERVER_IP=192.168.1.20
```

---

## [p1_shared] DB 접속 설정 (T-003: DbConnectionPool)

```env
# ─────────────────────────────────────────────────────
# KDMS (한국 주식 DB)
# ─────────────────────────────────────────────────────
# 개발PC DB (어느 PC에서 실행하든 이 주소로 개발PC DB에 접근)
DEV_KDMS_DB_HOST=${DEV_IP}
DEV_KDMS_DB_PORT=5432
DEV_KDMS_DB_NAME=kdms_db
DEV_KDMS_DB_USER=kdms_user
DEV_KDMS_DB_PASSWORD=your_password_here

# 서버PC DB (어느 PC에서 실행하든 이 주소로 서버PC DB에 접근)
SERVER_KDMS_DB_HOST=${SERVER_IP}
SERVER_KDMS_DB_PORT=5432
SERVER_KDMS_DB_NAME=kdms_db
SERVER_KDMS_DB_USER=kdms_user
SERVER_KDMS_DB_PASSWORD=your_password_here

# ─────────────────────────────────────────────────────
# USDMS (미국 주식 DB)
# ─────────────────────────────────────────────────────
# 개발PC DB
DEV_USDMS_DB_HOST=${DEV_IP}
DEV_USDMS_DB_PORT=5433
DEV_USDMS_DB_NAME=usdms_db
DEV_USDMS_DB_USER=usdms_user
DEV_USDMS_DB_PASSWORD=your_password_here

# 서버PC DB
SERVER_USDMS_DB_HOST=${SERVER_IP}
SERVER_USDMS_DB_PORT=5433
SERVER_USDMS_DB_NAME=usdms_db
SERVER_USDMS_DB_USER=usdms_user
SERVER_USDMS_DB_PASSWORD=your_password_here
```

---

## [p1_shared] 백업 설정 (T-007: BackupManager)

```env
# ─────────────────────────────────────────────────────
# 백업 파일 저장 디렉토리 (절대경로 또는 프로젝트 루트 기준 상대경로)
# ─────────────────────────────────────────────────────
BACKUP_DIR=./backups

# Docker 컨테이너명
KDMS_CONTAINER_NAME=p2_kdms_db
USDMS_CONTAINER_NAME=p3_usdms_db

# Docker 볼륨명
KDMS_VOLUME_NAME=kdms_pgdata
USDMS_VOLUME_NAME=usdms_pgdata
```

---

## [p1_shared] DB 동기화 설정 (T-008: SyncManager)
### 키 생성 명령 :
```bash
ssh-keygen -t rsa -b 4096 -f ~/.ssh/tdms_sync_rsa -N ""
```

```env
# ─────────────────────────────────────────────────────
# SSH 접속 설정 (개발PC ↔ 서버PC 동기화용)
# SSH 키는 절대 Git에 커밋하지 않습니다.
# ─────────────────────────────────────────────────────
SSH_USER=roid
SSH_KEY_PATH=~/.ssh/tdms_sync_rsa
```

---

## [p1_shared] API 키 설정 (T-004: KisApiCore, T-005: KiwoomApiCore)

```env
# ─────────────────────────────────────────────────────
# KIS (한국투자증권) API
# ─────────────────────────────────────────────────────
KIS_APP_KEY=your_kis_app_key_here
KIS_APP_SECRET=your_kis_app_secret_here
KIS_TOKEN_CACHE_PATH=~/.cache/tdms/kis_token.json

# ─────────────────────────────────────────────────────
# Kiwoom API
# ─────────────────────────────────────────────────────
KIWOOM_APP_KEY=your_kiwoom_app_key_here
KIWOOM_APP_SECRET=your_kiwoom_app_secret_here
KIWOOM_TOKEN_CACHE_PATH=~/.cache/tdms/kiwoom_token.json
```

---

## [p1_shared] 로거 설정 (T-001: ops/logger.py)

```env
# ─────────────────────────────────────────────────────
# 로그 파일 저장 디렉토리 (절대경로 또는 프로젝트 루트 기준 상대경로)
# ─────────────────────────────────────────────────────
LOG_DIR=./logs
LOG_LEVEL=INFO
```

---

## 코드에서의 로딩 방법

```python
# 모든 서브프로젝트 공통 사용 패턴
from dotenv import load_dotenv, find_dotenv

# 프로젝트 루트의 .env 자동 탐색 및 로드
load_dotenv(find_dotenv())
```
